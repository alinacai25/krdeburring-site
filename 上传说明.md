# krdeburring.com 上传说明

把本文件夹里的内容上传到 **www.krdeburring.com 网站根目录**即可，不需要 CMS 后台。

## 目录对应关系

| 本地文件 | 上传到服务器的位置 |
|---------|------------------|
| `index.html` | `/index.html`（**覆盖**首页，仅新增一个导航项） |
| `blog/index.html` | `/blog/index.html` |
| `blog/how-to-choose-a-metal-deburring-machine.html` | `/blog/...同名` |
| `blog/how-to-remove-burrs-from-laser-cut-parts.html` | `/blog/...同名` |
| `blog/deburring-vs-chamfering.html` | `/blog/...同名` |
| `blog/stainless-steel-deburring-guide.html` | `/blog/...同名` |
| `sitemap.xml` | `/sitemap.xml`（**覆盖**旧版） |

即：把 `blog/` 整个文件夹丢到网站根目录，再覆盖根目录的 `index.html` 和 `sitemap.xml`。

### 关于 index.html（首页）

基于线上首页做的**最小改动**，只在导航栏插入一行，其余内容一字未动：

```diff
         <a href="consumables.html" ...>Consumables</a>
+        <a href="blog/" data-cms-link="index/header/link-blog">Blog</a>
         <a href="about.html" ...>About Us</a>
```

改动由 `tools/patch_index_nav.py` 生成，可重复执行（已存在则跳过）。若你之后线上首页有更新，重新跑一次脚本即可，不会丢失改动。

## 上传方式（任选其一）

- **GitHub Pages**：站点托管在 GitHub Pages，直接把文件提交到对应仓库根目录即可自动发布。
- **FTP / 主机面板**：用 FileZilla 或主机商的文件管理器上传。

## 上传后验证（重要）

1. 打开 `https://www.krdeburring.com/` → 导航栏应多出 **Blog** 一项，点进去是文章列表
2. 打开 `https://www.krdeburring.com/blog/` → 应看到博客列表（4 篇文章）
3. 点开任意一篇 → 应正常显示，且文末"Related machines"链接指向 kr-machinery.com
4. 打开 `https://www.krdeburring.com/sitemap.xml` → 应看到 6 个 URL（首页 + 5 个博客页）

## 上传后要做的一件事

到 **Google Search Console** 提交新的 sitemap（`https://www.krdeburring.com/sitemap.xml`），让 Google 尽快发现这 4 篇文章。

> 注：该域名此前未被 Google 收录，提交 sitemap 是让它开始被抓取的最快方式。

## 关于首页

本方案**保留 krdeburring.com 首页不跳转**（作为博客站入口）。
若之后要让首页也跳转到 kr-machinery.com，见 `redirects/301-redirect.conf` 里的注释说明。
